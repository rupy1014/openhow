---
slug: 자동화입문-멀티에이전트
title: "멀티 에이전트"
nav: 멀티 에이전트
order: 2
---

`/write 3화`를 치면 한 명이 아니라 다섯 명이 일한다.

---

## 에이전트가 뭐야?

저번 글에서 만든 `.claude/agents/` 안의 파일들이야.

각각 역할이 고정된 작업자다. `model`, `description`이 지정돼 있고, `/write`가 실행되면 순서대로 넘겨받아 처리한다.

`model`을 적지 않으면 마스터가 쓰는 모델을 그대로 쓴다.
가볍게 테스트할 땐 그냥 비워두고 시작해도 돼.

:::canvas-flow
{
  "nodes": [
    { "id": "sm", "label": "series-manager\n컨텍스트 파악", "col": 0, "row": 0, "type": "process" },
    { "id": "ow", "label": "outline-writer\n서두 훅 + 본문 설계", "col": 1, "row": 0, "type": "process" },
    { "id": "wr", "label": "writer\n초안 작성", "col": 2, "row": 0, "type": "default" },
    { "id": "st", "label": "style-touch\n표현 다듬기", "col": 3, "row": 0, "type": "default" },
    { "id": "pr", "label": "proofreader\n검수 + 저장", "col": 4, "row": 0, "type": "success" }
  ],
  "edges": [
    { "from": "sm", "to": "ow" },
    { "from": "ow", "to": "wr" },
    { "from": "wr", "to": "st" },
    { "from": "st", "to": "pr" }
  ],
  "direction": "LR",
  "cols": 5,
  "rows": 1
}
:::

각 에이전트는 `CLAUDE.md`, `docs/tone-and-manner.md`, `series-plan.md`를 공통으로 참조한다.

---

## 왜 나눠놨어?

우리가 대화하는 Claude가 마스터 에이전트다. `/write`를 치면 마스터가 직접 글을 쓰는 게 아니라 담당자들에게 일을 넘긴다.

담당자(sub-agent)는 자기 역할만 처리하고 끝낸다. 마스터의 컨텍스트는 오염되지 않는다.

왜? 담당자는 매번 새로 시작해. 마스터의 이전 대화를 모른다.
기획이 초안에 끼어들거나, 퇴고 기준이 집필 중에 흔들리는 일이 없다.

:::canvas-sequence
{
  "participants": [
    { "id": "master", "label": "마스터" },
    { "id": "sm", "label": "series-manager" },
    { "id": "ow", "label": "outline-writer" },
    { "id": "pr", "label": "proofreader" }
  ],
  "messages": [
    { "from": "master", "to": "sm", "label": "3화 컨텍스트 파악", "type": "solid" },
    { "from": "sm", "to": "master", "label": "컨텍스트 완료", "type": "dashed" },
    { "from": "master", "to": "ow", "label": "아웃라인 작성", "type": "solid" },
    { "from": "ow", "to": "master", "label": "아웃라인 완료", "type": "dashed" },
    { "from": "master", "to": "pr", "label": "검수 요청", "type": "solid" },
    { "from": "pr", "to": "master", "label": "80점 미만 → 재실행", "type": "dashed", "note": { "over": "pr", "text": "기준 미달 시 되돌아감" } }
  ]
}
:::

마스터가 직접 다 하면 기획, 초안, 퇴고가 한 대화에 뒤섞인다.

대화가 길어질수록 Claude는 앞에서 정한 규칙을 잊어.
기획 때 정한 말투를 초안 쓸 때 놓치거나, 퇴고 기준을 나중엔 기억 못 하거나.

담당자에게 시키면 각자 깨끗한 컨텍스트로 집중한다.

80점 미만이면 `proofreader`가 결과를 마스터에게 돌려준다. 마스터가 `style-touch`를 다시 실행한다. 담당자끼리 직접 소통하지 않는다 — 항상 마스터를 거친다.

---

## 한 줄 정리

`/write`는 다섯 에이전트가 순서대로 처리하는 파이프라인이다. 기준 미달이면 자동으로 재실행된다.
