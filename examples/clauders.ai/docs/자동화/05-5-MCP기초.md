---
slug: 자동화입문-MCP기초
title: "MCP 기초"
nav: MCP 기초
order: 5
---

**Claude Code가 기본으로 할 수 있는 건 파일 읽기, 쓰기, 터미널 실행이야. MCP를 연결하면 바깥 세상과 소통할 수 있어.**

:::youtube eEtZENgCux4
:::

Slack에 메시지 보내기, Google Drive 파일 읽기, 웹 검색, 데이터베이스 조회 — 이런 걸 Claude가 직접 해.

---

## MCP가 뭐야?

**Model Context Protocol** — AI가 외부 도구에 연결되는 표준 규격이야.

비유하면 **USB-C**야. 예전엔 기기마다 충전기가 달랐잖아. USB-C가 나오니까 하나로 다 꽂아.

MCP도 똑같아. 예전엔 AI 도구마다 Slack 연동을 따로 만들었어. MCP가 나오니까 하나의 규격으로 뭐든 연결해.

Anthropic이 2024년에 만들었어. 지금은 Linux Foundation 소속이고, OpenAI랑 Google도 지원해. 업계 공통 표준이 된 거야.

---

## 뭘 할 수 있어?

| MCP 서버 | 하는 일 |
|----------|---------|
| **Notion** | 페이지 만들기, 데이터베이스 검색, 내용 수정 |
| **Slack** | 채널 읽기, 메시지 보내기, 스레드 요약 |
| **Google Drive** | 파일 검색, 문서 읽기, 정리 |
| **Brave Search** | 실시간 웹 검색 |
| **GitHub** | PR 만들기, 이슈 관리, 코드 리뷰 |
| **PostgreSQL** | 데이터베이스 조회, 스키마 확인 |

이 외에도 200개 이상의 MCP 서버가 있어.

---

## 어떻게 연결해?

Claude Code 터미널에서 한 줄이면 돼.

**Notion 연결하기:**

```bash:터미널
claude mcp add --transport http notion https://mcp.notion.com/mcp
```

**웹 검색 연결하기:**

```bash:터미널
claude mcp add --transport stdio brave-search --env BRAVE_API_KEY=네API키 -- npx -y @modelcontextprotocol/server-brave-search
```

연결 뒤에 Claude Code를 다시 시작하면 바로 써. `/mcp` 치면 연결된 서버 목록이 나와.

---

## 실제로 어떻게 써?

MCP를 연결하면 그냥 말로 시키면 돼.

**Notion에 회의록 정리:**

```text:Claude 프롬프트
오늘 회의 내용 정리해서 Notion "회의록" 데이터베이스에 새 페이지로 만들어줘.
참석자: 김팀장, 박대리
결정사항: 다음 주 수요일까지 프로토타입 완성
```

**Slack 채널 요약:**

```text:Claude 프롬프트
#general 채널에서 오늘 대화 요약해줘.
```

Claude가 Notion이나 Slack에 직접 접근해서 처리해.

---

## 기본 도구랑 뭐가 달라?

Claude Code에는 처음부터 쓸 수 있는 기본 도구가 있어.

| 기본 도구 | MCP 서버 |
|-----------|----------|
| 파일 읽기/쓰기 | Notion, Google Drive |
| 터미널 명령어 | Slack, Gmail |
| 웹 검색 (기본) | Brave Search (더 강력) |
| 코드 검색 | GitHub, PostgreSQL |

기본 도구는 **내 컴퓨터 안**에서만 작동해. MCP는 **인터넷 너머의 서비스**와 연결해주는 거야.

---

## 설정은 어디에 저장돼?

| 범위 | 어디에 | 언제 써 |
|------|--------|--------|
| 나만 (기본) | `~/.claude.json` | 혼자 쓸 때 |
| 프로젝트 공유 | `.mcp.json` | 팀이 같이 쓸 때 |

API 키 같은 비밀 정보는 `.mcp.json`에 직접 넣지 마. 환경변수로 관리해.

---

## 지금 해야 해?

아니. 처음부터 필요하진 않아.

기본 도구만으로도 충분히 많은 걸 할 수 있어. Notion이나 Slack 연동이 필요해지면 그때 연결하면 돼.

[AI로 일하기](../AI로일하기/00-AI로일하기) 섹션의 **프로젝트 브레인**에서 MCP를 실전으로 써볼 거야.

---

## 한 줄 정리

MCP는 Claude Code에 외부 서비스를 연결하는 USB-C야. 한 줄이면 연결되고, 말로 시키면 돼.
